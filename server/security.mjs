import {createHmac,timingSafeEqual} from 'node:crypto';
export function validSignature(id,requestId,signature,secret){if(!id||!requestId||!signature||!secret)return false;const fields=Object.fromEntries(signature.split(',').map(p=>p.trim().split('=')));if(!/^\d+$/.test(fields.ts||'')||!/^[a-f0-9]{64}$/i.test(fields.v1||''))return false;const manifest=`id:${String(id).toLowerCase()};request-id:${requestId};ts:${fields.ts};`;const expected=createHmac('sha256',secret).update(manifest).digest();return timingSafeEqual(expected,Buffer.from(fields.v1,'hex'))}
export function paymentMatches(payment,order,collector,live){return payment.external_reference===order.id && String(payment.collector_id)===String(collector) && payment.currency_id==='BRL' && Math.round(Number(payment.transaction_amount)*100)===Math.round(Number(order.amount)*100) && payment.live_mode===live && String(payment.metadata?.user_id)===order.user_id && String(payment.metadata?.product_id)===order.product_id}
export function isPaid(payment){return payment.status==='approved' && Number(payment.transaction_amount_refunded||0)===0}
